import React from "react";
import "./TestScheduleManager.css";

const TestScheduleManager: React.FC = () => {
    return (
        <div className="test-schedule-manager">
            <h3>Test Scheduling</h3>
            <p>Scheduling options will be available here soon (daily, on boot, on event, etc).</p>
        </div>
    );
};

export default TestScheduleManager;
